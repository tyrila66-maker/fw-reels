{
  "name": "reels-pipeline",
  "version": "1.0.0",
  "private": true,
  "description": "Автоматический конвейер: тема -> сценарий (Claude) -> озвучка (ElevenLabs) -> фон (Pexels) -> вертикальный MP4 с субтитрами (ffmpeg)",
  "main": "src/index.ts",
  "scripts": {
    "generate": "ts-node src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.109.0",
    "axios": "^1.18.1",
    "dotenv": "^17.4.2",
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0"
  },
  "devDependencies": {
    "@types/ffprobe-static": "^2.0.3",
    "@types/node": "^26.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
