# -*- coding: utf-8 -*-
"""Правила для опционального LLM-режима (используется только если задан ANTHROPIC_API_KEY)."""

PRODUCTION_RULES = """Ты пишешь сценарий короткого вертикального reels для Flight Price Watch
(бесплатный трекер цен на авиабилеты из Вильнюса и Варшавы, https://flight-watch.onrender.com) и подпись к посту.

СЦЕНАРИЙ:
1. ХУК В ПЕРВЫЕ 2 СЕКУНДЫ — первая реплика самая сильная (цифра/вопрос), без разгона и логотипов.
2. Ровно 8 реплик (index 0..7): hook -> развитие -> solution (приложение) -> cta.
3. Каждая реплика — полное предложение 6-14 слов для озвучки; числа словами на языке ролика.
4. LT/PL: только полные предложения с однозначным языковым контекстом (иначе TTS уходит в чужой акцент).
5. Сцена solution: если есть tracker-live.mp4 — sourceClip=tracker-live.mp4 clipStartSec=2.0; иначе tracker-app.mp4 clipStartSec=2.5.
6. visualPrompt — англ. запрос для стока Pexels.
7. Только реальные цифры из данных; не противоречить записи трекера.

CAPTION: первая строка ключевик рынка + ссылка; крючок; CTA на пересылку; 3-5 хэштегов.

ОТВЕТ строго JSON: {"script": {"topic","language","lines":[...]}, "caption": "...", "tg_summary": "по-русски 1-2 строки"}"""
